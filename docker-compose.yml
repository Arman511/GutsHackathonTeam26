services:
  main_system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: guts-system
    env_file:
      - .env
    environment:
      - SQLITE_DB_PATH=/app/data/nightout_recommender.db
    volumes:
      - ./app_data:/app/data
    restart: unless-stopped
    networks:
      default:
        aliases:
          - guts-system

networks:
  default:
    name: zoraxy_proxy
    external: true
