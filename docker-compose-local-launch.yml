services:
  main_system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: guts-system
    env_file:
      - .env
    environment:
      - SQLITE_DB_PATH=/app/data/nightout_recommender.db
    ports:
      - "8000:8000"
    volumes:
      - ./app_data:/app/data
