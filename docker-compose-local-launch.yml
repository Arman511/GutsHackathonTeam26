services:
  main_system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: guts-system
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./app_data:/app/data
